<?xml version="1.0" encoding="UTF-8"?>
<!-- Copyright Nixort <https://github.com/Nixort> 2026. -->
<!-- License: GNU General Public License v3.0 only. -->
<!-- Federated CFR Connect Protocol (FCP). -->
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>io.github.nixort.libfcp.tests</groupId>
  <artifactId>libfcp-maven-consumer-smoke</artifactId>
  <version>1.0.0-rc.1</version>

  <properties>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <libfcp.version>1.0.0-rc.1</libfcp.version>
  </properties>

  <repositories>
    <repository>
      <id>libfcp-local</id>
      <url>${libfcp.localRepository}</url>
    </repository>
  </repositories>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.13.0</version>
        <configuration>
          <release>${maven.compiler.release}</release>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <dependencies>
    <dependency>
      <groupId>io.github.nixort</groupId>
      <artifactId>libfcp</artifactId>
      <version>${libfcp.version}</version>
    </dependency>
    <dependency>
      <groupId>io.github.nixort</groupId>
      <artifactId>libfcp</artifactId>
      <version>${libfcp.version}</version>
      <classifier>linux-x86_64</classifier>
    </dependency>
  </dependencies>
</project>
